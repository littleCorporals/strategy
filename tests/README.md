# Tests

这里放置 API、缓存、模型后台、训练流水线、每日推理和次日验证的最小回归测试。

当前覆盖：

- `/api/status` 状态接口和 migrations 初始化。
- `/api/daily` 数据库优先、缺失后拉取并写入本地缓存。
- `/api/admin/overview`、训练任务排队。
- 模型注册、上线、下线、归档生命周期。
- 训练任务执行后注册候选模型并落盘 artifact。
- 激活模型生成每日预测，并用下一交易日数据写入验证结果。
- 模型生命周期事件审计、上一版回滚、审批通过/拒绝、模型表现聚合。
