# 改造进度清单

更新时间：2026-06-05

## 目标

把当前股票决策台从可运行 MVP 改造成可长期扩展的标准工程：业务、数据、第三方客户端、模型工程、模型管理后台分层清晰，后续可以平滑接入训练流水线、模型版本上线/下线、每日推理和次日验证。

## 架构原则

- `app/main.py` 只负责应用装配，不承载业务逻辑。
- `app/api/routes/` 只处理 HTTP 入参和出参，业务计算逐步下沉到 `app/services/`。
- `app/clients/` 只放第三方服务适配器，包括 Tushare、巨潮资讯、AI 接口。
- `app/repositories/` 只放数据访问和缓存仓储，避免路由层直接散落 SQL/缓存策略。
- `modeling/` 只放模型工程能力，不反向依赖 `app`。
- 所有模型版本、训练任务、推理结果、验证结果必须入库，可追溯、可回滚。

## 已完成

- [x] 建立 SQLite 本地缓存，默认文件：`data/market_cache.sqlite3`。
- [x] 行情查询改为数据库优先，缺失且满足收盘刷新规则才请求第三方。
- [x] 前端只请求后端 `/api/*`，不直连第三方数据服务。
- [x] 增加模型相关表：`ml_models`、`ml_training_runs`、`ml_pipeline_runs`、`ml_predictions`、`ml_validation_results`。
- [x] 增加模型管理台 MVP：`/admin`、`/api/admin/overview`、训练任务排队。
- [x] `app/main.py` 瘦身为应用工厂和路由装配。
- [x] 市场业务路由迁移到 `app/api/routes/market.py`。
- [x] 创建标准目录：`app/api/routes`、`app/services`、`app/clients`、`app/repositories`、`app/schemas`、`migrations`、`tests`。
- [x] 修正 `modeling` 反向依赖 `app.db` 的问题。
- [x] 拆出 Tushare 客户端：`app/clients/tushare_client.py`，统一 SDK 初始化、代理、限流、冷却、DataFrame 转 records。
- [x] 拆出巨潮资讯客户端：`app/clients/cninfo_client.py`，统一股票映射、公告查询、HTTP JSON 请求。
- [x] 拆出 AI 客户端：`app/clients/ai_client.py`，统一 AI 配置判断、推荐请求、JSON 解析和错误类型。
- [x] 拆行情服务：日线、指标、资金流、股票基础资料缓存策略下沉到 `app/services/market_data.py`。
- [x] 拆分析服务：趋势、资金、估值、风险、支撑压力下沉到 `app/services/analysis.py`。
- [x] 拆推荐服务：推荐池、消息源核验、AI/规则推荐下沉到 `app/services/recommendation.py`。
- [x] 拆回测服务：短线策略、历史窗口、验证聚合下沉到 `app/services/backtest.py`。
- [x] 拆数据库仓储：新增 `app/repositories/market_cache.py`，路由和服务不再直接依赖 `app/db/cache.py`。
- [x] 拆个股决策编排：分析 payload、消息源、AI/规则推荐组合下沉到 `app/services/stock_decision.py`。
- [x] 增加 API schemas MVP：主要市场接口挂载 `app/schemas/market.py` response model。
- [x] 增加数据库 migrations MVP：`app/db/migrations.py` 执行 `migrations/*.sql`，并通过 `schema_migrations` 追踪版本。
- [x] 增加最小自动化测试：状态接口、缓存优先策略、缺失后拉取并入库、后台概览、训练任务排队。
- [x] 修正个股分析实际交易日语义：接口同时返回请求日期和实际命中的行情日期，并按实际行情日期查询资金/估值/消息源。
- [x] 增加模型版本生命周期 MVP：注册候选模型、激活上线、下线为 inactive、归档 archived，并在后台页面提供操作入口。
- [x] 增加模型训练流水线 MVP：从本地缓存日线构造样本、生成基础特征/标签、训练基线模型、落盘 artifact、注册候选模型并记录 pipeline run。
- [x] 增加每日推理和次日验证 MVP：激活模型生成 `ml_predictions`，下一交易日数据可用后写入 `ml_validation_results`。
- [x] 增加模型生命周期审计和回滚 MVP：新增 `ml_model_events`，记录注册、上线、替换、下线、归档、artifact 更新和回滚事件，后台支持回滚上一版。
- [x] 增加模型上线审批 MVP：候选模型需审批通过后才能激活上线，支持拒绝状态并记录审批/拒绝事件。
- [x] 增加模型效果监控 MVP：按模型聚合预测数、验证数、命中率、平均次日收益，并在后台展示。
- [x] 优化模型后台新手交互：首屏增加“建议下一步”和四步操作向导，常用操作自动选择待处理任务/模型，复杂明细下沉到后续面板。
- [x] 改造模型后台为标准管理系统布局：左侧菜单、右侧内容区，工作台、训练任务、模型版本、预测验证、效果监控、审计日志、基础配置分开展示。
- [x] 加固本地运行安全边界：收紧 CORS 默认来源、隐藏状态接口敏感运行信息、限制管理接口本机访问、限制模型 artifact 读取目录。
- [x] 重构主看盘台为新手工作台：按“市场判断、候选池、当前观察、明日计划、资金/消息解释”组织，不再沿用旧版布局。
- [x] 主看盘台动态渲染改为 DOM/textContent 优先，降低第三方行情、公告和模型文本造成的前端注入风险。
- [x] 主看盘台改造为后台管理系统布局：左侧菜单、顶部工具栏、候选表格、右侧个股详情和下方分析面板。
- [x] 主看盘台改造为独立后台模块：市场总览、候选列表、个股分析、行业趋势、数据工具分别用独立界面切换，不再依赖长页面滚动定位。
- [x] 增加模型准确度首轮优化：新增 `next_high_2pct_v1`、`next_3d_high_3pct_v1` 标签，训练和次日验证输出 Top 20/50/100 命中率、全市场基准和相对提升，模型后台效果监控展示 Top50 排序表现。
- [x] 优化模型后台训练验证交互：自动推荐预测日、自动寻找最近可验证预测日、增加验证对比日/状态提示和“一键训练并上线”，日常预测验证无需手填交易日。
- [x] 升级模型训练器：新增 `short_swing_v2` 30 个增强日线特征，训练器从均值差线性打分升级为 `logistic_ranker_v1`，记录迭代轮次、最佳轮次、log loss 和训练历史。
- [x] 图表化模型指标页：模型后台“模型指标”增加 TopN 排序柱状图、训练 Log Loss 曲线、特征权重条形图和数据验证链路视图，并收紧后台通用表格、按钮和响应式布局。
- [x] 回填最近 40 个完整交易日的全市场日线缓存，重新训练 `short_swing_v2 / next_high_3pct_v1`、`next_high_2pct_v1`、`next_3d_high_3pct_v1` 三个候选模型，并保守上线同标签的新 `next_high_3pct_v1` 模型。
- [x] 增加历史日线回填和训练矩阵能力：后台支持按日期范围补齐缺失日线，并按多个训练窗口、标签集重复训练，自动按验证集 Top50 提升、命中率、F1 和验证样本数选择候选，可一键审批并上线最优模型。
- [x] 补全模型后台训练可见性：训练任务列表直接展示样本数、验证样本、验证 F1、Log Loss、Top50 命中率、Top50 提升和参数摘要；模型指标页增加训练参数面板。
- [x] 补全模型版本列表指标可见性：模型列表直接展示总样本、验证样本、验证 F1、Log Loss、Top50 命中率和 Top50 提升；当前使用中模型独立置顶并用图表展示核心指标，其它模型改为紧凑列布局和分页展示，减少进入详情页才能看分数、横向滚动和历史模型堆叠的操作成本。
- [x] 增加模型后台字段说明提示：训练任务表、训练配置、历史回填、模型指标和训练参数字段旁增加可悬停/聚焦的 `?` 说明，解释 F1、Log Loss、Top50、提升、样本、参数等含义。
- [x] 整理主看盘台和模型后台菜单界面布局：主看盘台统一各菜单工作区容器、顶部工具栏和面板间距；模型后台训练页改为上方操作区、下方整宽训练记录，修正字段提示在表单和表格中的排列。
- [x] 统一日期输入交互：主看盘台和模型后台所有交易日、起止日、回填日、预测日和验证日均改为日期选择器，并禁止键盘手输和粘贴日期。
- [x] 优化模型后台图表显示和模型版本页排版：当前使用中模型指标改为紧凑仪表卡，模型列表移动端改为卡片式行，模型指标页 TopN、Loss 曲线和特征权重图表补齐稳定画布和响应式布局。
- [x] 加固训练任务重复执行保护：同一个训练任务只允许从 queued/failed 原子切换到 running，已完成任务重复执行时复用已有模型，避免重复注册多条指标完全相同的候选模型。
- [x] 修正模型后台训练步骤顺序：工作台和单模型训练流程补充“回填历史行情/准备训练样本”作为训练前置步骤，空库时优先引导回填历史日线。
- [x] 修复模型后台字段说明 tooltip：移除问号原生 `title` 提示冲突，改为挂载到页面根部的统一浮层，避免表格和卡片 overflow 裁切、颜色继承异常和窄屏超宽。
- [x] 修正模型后台训练页排版错位：训练步骤和多模型选优卡片按可用宽度自适应换列，历史回填日期字段、按钮和说明文字不再撑破卡片，移动端多模型选优改为单列展示。
- [x] 修复模型后台长任务“没反应”问题：训练和多模型选优改为工作线程执行，避免单个训练请求卡住后台概览；回填、训练和选优按钮增加持续状态条和忙碌文案。
- [x] 增加模型后台长任务进度条：历史回填、单模型训练、一键训练并上线、多模型选优统一展示阶段、预计进度、已等待时间和完成后自动刷新提示，减少长时间等待的不确定感。
- [x] 完成模型后台全界面样式复查：模型版本列表改为卡片行，模型指标页补齐小桌面响应式断点，特征权重数值列固定定位，预测验证页操作卡片整宽展示，并补充 hash 菜单切换监听，避免刷新或切换时停留在旧页面。
- [x] 修正模型后台工作台步骤卡片文本间距：新手步骤的标题、副标题、状态徽标、正文和按钮重新分层排布，状态徽标与标题文字列对齐，宽屏、桌面、平板和移动端按可用宽度自适应换行。
- [x] 去除模型后台长任务重复提示：回填、单模型训练、一键训练并上线、多模型选优已有进度卡时不再同步弹出底部 toast，只保留进度卡和错误 toast。

## 待改造

- [ ] 细化 API schemas：把宽松的 `dict[str, Any]` 响应逐步收敛成明确字段 schema。
- [ ] 按 `docs/模型训练迭代改进清单.md` 推进 P0：补齐 240 日以上数据覆盖、统一股票池过滤、增加可交易标签/风险标签、滚动验证、上线门槛和自动拦截。
- [ ] 推进 `short_swing_v3`：纳入 `daily_basic`、`moneyflow`、行业强弱、市场环境、风险特征，并保存特征缺失率和版本冻结信息。
- [ ] 引入表格模型候选：保留 `logistic_ranker_v1` 基线，新增树模型候选并在同区间滚动验证后再允许上线。
- [ ] 完整模型训练流水线：特征版本冻结、时间序列交叉验证、模型评估报告、失败重试和定时调度。
- [ ] 完整模型上线/下线流程：灰度、禁用、自动告警和更细粒度权限。

## 验收记录

- [x] 2026-05-28：`python -m compileall app modeling` 通过。
- [x] 2026-05-28：`GET /api/status` 通过。
- [x] 2026-05-28：`GET /api/admin/overview` 通过。
- [x] 2026-05-28：`GET /` 和 `GET /admin` 页面入口返回 200。
- [x] 2026-05-28：拆出 Tushare 客户端后重启服务，`GET /api/status`、`GET /api/admin/overview`、`GET /`、`GET /admin` 通过。
- [x] 2026-05-28：拆出巨潮资讯和 AI 客户端后，`python -m compileall app modeling` 通过。
- [x] 2026-05-28：拆出巨潮资讯和 AI 客户端后重启服务，`GET /api/status`、`GET /api/admin/overview`、`GET /`、`GET /admin` 通过。
- [x] 2026-05-28：拆出行情、分析、推荐、回测服务和缓存仓储后，`python -m compileall app modeling` 通过。
- [x] 2026-05-28：服务化后 `python -c "from app.main import app"` 通过。
- [x] 2026-05-28：服务化后重启服务，`GET /api/status`、`GET /api/admin/overview`、`GET /`、`GET /admin` 通过。
- [x] 2026-05-28：服务化后业务接口烟测，`GET /api/daily`、`GET /api/backtest/short-swing`、`GET /api/industry-trends` 通过。
- [x] 2026-05-28：清理部分路由薄包装后，`python -m compileall app modeling` 通过。
- [x] 2026-05-28：拆个股决策编排并增加市场 response schemas 后，`python -m compileall app modeling` 和 `python -c "from app.main import app"` 通过。
- [x] 2026-05-28：增加 response schemas 后重启服务，`GET /api/status`、`GET /api/daily`、`GET /api/backtest/short-swing`、`GET /api/stock/000001.SZ/history`、`GET /api/stock/000001.SZ/analysis`、`GET /api/query/daily`、`GET /`、`GET /admin` 通过。
- [x] 2026-05-28：增加 migrations MVP 后，`schema_migrations` 已记录 `0001_initial_schema`。
- [x] 2026-05-28：增加最小自动化测试后，`python -m pytest` 通过，5 个用例全部通过。
- [x] 2026-05-28：增加最小自动化测试后，`python -m compileall app modeling tests` 通过。
- [x] 2026-05-28：修正个股分析实际交易日语义后，`python -m pytest` 通过，6 个用例全部通过。
- [x] 2026-05-28：修正个股分析实际交易日语义后，`python -m compileall app modeling tests` 通过。
- [x] 2026-05-28：增加模型版本生命周期 MVP 后，`python -m pytest` 通过，7 个用例全部通过。
- [x] 2026-05-28：增加模型版本生命周期 MVP 后，`python -m compileall app modeling tests` 通过。
- [x] 2026-05-28：增加模型训练流水线 MVP 后，`python -m pytest` 通过，8 个用例全部通过。
- [x] 2026-05-28：增加模型训练流水线 MVP 后，`python -m compileall app modeling tests` 通过。
- [x] 2026-05-28：增加每日推理和次日验证 MVP 后，`python -m pytest` 通过，9 个用例全部通过。
- [x] 2026-05-28：增加每日推理和次日验证 MVP 后，`python -m compileall app modeling tests` 通过。
- [x] 2026-05-28：增加模型生命周期审计和回滚 MVP 后，`python -m pytest` 通过，10 个用例全部通过。
- [x] 2026-05-28：增加模型生命周期审计和回滚 MVP 后，`python -m compileall app modeling tests` 通过。
- [x] 2026-05-28：增加模型上线审批 MVP 后，`python -m pytest` 通过，11 个用例全部通过。
- [x] 2026-05-28：增加模型上线审批 MVP 后，`python -m compileall app modeling tests` 通过。
- [x] 2026-05-28：增加模型效果监控 MVP 后，`python -m pytest` 通过，11 个用例全部通过。
- [x] 2026-05-28：增加模型效果监控 MVP 后，`python -m compileall app modeling tests` 通过。
- [x] 2026-05-28：优化模型后台新手交互后，`python -m pytest` 通过，11 个用例全部通过。
- [x] 2026-05-28：优化模型后台新手交互后，`GET /admin` 返回 200。
- [x] 2026-05-28：改造模型后台菜单式布局后，`python -m pytest` 通过，11 个用例全部通过。
- [x] 2026-05-28：改造模型后台菜单式布局后，`python -m compileall app modeling tests` 通过，`GET /admin` 返回 200。
- [x] 2026-05-28：模型后台标准管理布局二次优化后，`python -m pytest`、`python -m compileall app modeling tests` 通过，Edge 桌面/移动端截图检查通过。
- [x] 2026-05-29：本地安全加固和主看盘台重构后，`python -m pytest` 通过，13 个用例全部通过。
- [x] 2026-05-29：本地安全加固和主看盘台重构后，`python -m compileall app modeling tests`、`node --check app/static/app.js` 通过。
- [x] 2026-05-29：本地服务 `GET /`、`GET /admin`、`GET /api/status`、`GET /api/daily?trade_date=20260528&auto_fallback=true` 返回 200。
- [x] 2026-05-29：Chrome headless 桌面/移动端截图检查通过，主看盘台首屏非空且主要工作区可见。
- [x] 2026-05-29：主看盘台后台管理布局改造后，`python -m pytest` 通过，13 个用例全部通过。
- [x] 2026-05-29：主看盘台后台管理布局改造后，`python -m compileall app modeling tests`、`node --check app/static/app.js` 通过。
- [x] 2026-05-29：后台管理布局页面 `GET /`、`GET /admin`、`GET /api/status`、`GET /api/daily?trade_date=20260528&auto_fallback=true` 通过，Chrome headless 桌面/移动端截图检查通过。
- [x] 2026-05-29：修复主看盘台后台布局中候选表格下方被右侧详情栏撑出大空白的问题，Chrome headless 桌面截图检查通过。
- [x] 2026-05-29：继续修复同类等高网格问题：模型后台基础配置拆为独立定义面板，主看盘台底部信息区改为独立流式列，移动端阶段/日志行避免状态徽标裁切。
- [x] 2026-05-29：修复主看盘台底部行业/指标/消息区仍被右侧详情栏高度推迟显示的问题，将底部信息区移入左侧主列，宽屏截图确认紧跟价格走势显示。
- [x] 2026-05-29：修复主看盘台左侧菜单在内部滚动容器下点击无效/不明显的问题，菜单改为 button + JS 定位滚动，并更新静态资源版本避免浏览器缓存旧脚本。
- [x] 2026-05-29：主看盘台独立模块改造后，`node --check app/static/app.js`、`python -m compileall app modeling tests`、`python -m pytest` 通过；Chrome headless 检查市场总览、候选列表、个股分析、行业趋势、数据工具桌面/移动端截图通过。
- [x] 2026-05-29：修复个股分析页超宽屏卡片横向摊开和普通桌面横向溢出问题，改为左摘要/中走势与消息/右决策资金的独立三栏工作区，并完成宽屏、桌面、移动端截图检查。
- [x] 2026-06-01：模型准确度首轮优化后，`python -m pytest` 通过，14 个用例全部通过；`python -m compileall app modeling tests`、`node --check app/static/admin.js` 通过。
- [x] 2026-06-01：模型后台交互优化后，`python -m pytest` 通过，15 个用例全部通过；`python -m compileall app modeling tests`、`node --check app/static/admin.js` 通过；`GET /admin`、`GET /api/admin/overview` 返回 200。
- [x] 2026-06-01：模型训练器升级后，`python -m pytest` 通过，16 个用例全部通过；`python -m compileall app modeling tests`、`node --check app/static/admin.js` 通过；本地真实训练生成 `logistic_ranker_v1 / short_swing_v2`，30 个特征、140 轮迭代、最佳轮次 105。
- [x] 2026-06-01：模型指标页图表化改造后，`node --check app/static/admin.js`、`python -m compileall app modeling tests`、`python -m pytest` 通过；`GET /admin`、`GET /api/admin/model-diagnostics` 返回 200；本地浏览器截图检查 `/admin#metrics` 通过。
- [x] 2026-06-02：回填 40 个完整交易日后，`short_swing_v2 / next_high_3pct_v1` 训练样本 189603、验证样本 37921、验证 F1 50.42%、Top50 命中率 64.00%、Top50 相对全市场提升 34.93 个百分点；新模型 `model_20260602_104450_15f041` 已审批并激活，`20260601` 预测已重新生成 500 条。
- [x] 2026-06-02：增加历史回填和训练矩阵接口后，`python -m pytest` 通过，18 个用例全部通过；`python -m compileall app modeling tests`、`node --check app/static/admin.js` 通过。
- [x] 2026-06-02：执行训练矩阵比较 `next_high_3pct_v1`、`next_high_2pct_v1`、`next_3d_high_3pct_v1` 三个标签，最优模型 `model_20260602_135126_264579` 已激活；验证样本 35712、验证 F1 59.81%、Top50 命中率 78.00%、Top50 相对全市场提升 37.57 个百分点，`20260601` 预测已用新模型重新生成 500 条。
- [x] 2026-06-02：补全训练任务和模型指标页的指标/参数展示后，`python -m pytest` 通过，18 个用例全部通过；`python -m compileall app modeling tests`、`node --check app/static/admin.js` 通过；`GET /admin` 返回 200 且包含“验证 F1”“训练参数”“Top50”。
- [x] 2026-06-02：增加字段说明提示后，`python -m pytest` 通过，18 个用例全部通过；`python -m compileall app modeling tests`、`node --check app/static/admin.js` 通过；`GET /admin` 返回 200 且包含 `help-tip` 和 `help-popover`。
- [x] 2026-06-02：整理菜单界面布局后，主看盘台和模型后台桌面/移动端 Chrome headless 截图非空，`GET /`、`GET /admin`、`GET /api/status`、`GET /api/admin/overview` 返回 200。
- [x] 2026-06-02：日期字段选择器改造后，`node --check app/static/app.js`、`node --check app/static/admin.js` 通过；页面中除股票代码外不再存在文本型日期输入。
- [x] 2026-06-03：主看盘台候选列表增加“实时”百度搜索入口，个股分析页已选股票顶部增加百度实时行情按钮；`node --check app/static/app.js`、`GET /`、`GET /static/app.js` 通过，Chrome headless 确认候选行生成百度链接。
- [x] 2026-06-03：验证百度普通搜索页会触发安全验证后，将候选列表和个股分析页入口改为百度股市通直链 `https://gushitong.baidu.com/stock/ab-{code}`，避免先落到搜索结果页。
- [x] 2026-06-03：按 A 股 T+1 和“14:30 尾盘候选”场景新增 `/api/recommendations/late-session`，叠加实时行情、计划买点、止损目标和追高状态；主看盘台候选列表增加“尾盘”分类，`python -m pytest` 通过，19 个用例全部通过。
- [x] 2026-06-03：重新执行训练矩阵并上线最优模型 `model_20260603_142103_0a43d8`，标签集为 `next_high_3pct_v1`，样本数 195107、验证 F1 46.04%、验证 accuracy 68.76%；已用新模型对 `20260602` 生成 500 条预测。
- [x] 2026-06-03：新增 `/api/recommendations/ml-late-session`，将激活模型预测 Top500 作为召回层，再叠加实时行情风险闸门，过滤北交所、ST/退市/U/W、实时走弱、过热、低成交额、低日内位置和 T+1 追高风险；主看盘台候选列表增加“模型尾盘”分类。
- [x] 2026-06-03：模型尾盘实时风险闸门后，`python -m pytest -q` 通过，20 个用例全部通过；`node --check app/static/app.js`、`python -m compileall app modeling tests` 通过；本地服务重启后 `/api/recommendations/ml-late-session?trade_date=20260602&limit=15&prediction_limit=500` 返回 15 条前排候选。
- [x] 2026-06-03：主看盘台“模型尾盘”增加模型参考卡，展示模型版本、样本量、验证 Accuracy/F1/LogLoss、验证 Top50 命中率、市场基准、最近预测验证 Top50 和拟合差；候选行增加模型排名、模型概率和实时过滤分。
- [x] 2026-06-03：整理模型训练迭代改进清单，新增 `docs/模型训练迭代改进清单.md`，明确 P0/P1/P2 执行顺序、验收标准和不得跳过的硬规则；同步更新模型优化方案和总进度待改造项。
- [x] 2026-06-04：模型版本列表补充样本、验证 F1、Log Loss、Top50 命中率和 Top50 提升；当前使用中模型独立置顶并以横向柱状图展示 F1、Top50、提升和 Loss，其它模型压缩为 5 列、增加每页 6 个的前端分页、取消模型页表格内层滚动；`node --check app/static/admin.js`、`python -m compileall app modeling tests`、`python -m pytest -q` 通过，20 个用例全部通过；Edge headless 截图确认 `/admin#models` 指标首屏可见且无表格内部滚动条。
- [x] 2026-06-05：优化模型后台图表和排版后，`node --check app/static/admin.js`、`python -m pytest -q` 通过，20 个用例全部通过；`GET /admin`、`GET /api/admin/overview` 返回 200；Playwright Chromium 截图确认 `/admin#models`、`/admin#metrics` 桌面视图和 `/admin#models` 移动端视图无明显文字挤压和图表溢出。
- [x] 2026-06-05：排查模型版本“提升”重复显示，确认 2026-06-04 同一训练任务被重复执行导致多条候选模型共享同一套训练指标；新增训练任务原子 claim 和已完成复用保护，`python -m compileall app modeling tests`、`node --check app/static/admin.js`、`python -m pytest -q` 通过，20 个用例全部通过。
- [x] 2026-06-05：补充训练前置数据准备步骤后，`node --check app/static/admin.js`、`python -m pytest -q` 通过，20 个用例全部通过；`GET /admin` 返回 200 且静态资源版本为 `20260605-data-step-1`。
- [x] 2026-06-05：按用户要求清空 `data/` 下所有运行数据和旧备份，服务重启后空库自动初始化；修复字段说明 tooltip 后，`node --check app/static/admin.js`、`python -m compileall app modeling tests`、`python -m pytest -q` 通过，20 个用例全部通过；桌面和移动端浏览器检查确认 `.help-tip` 无 `title`、浮层白字深底且不超出屏幕。
- [x] 2026-06-05：训练页排版复查后，`node --check app/static/admin.js`、`python -m compileall app modeling tests`、`python -m pytest -q` 通过，20 个用例全部通过；`GET /admin`、`GET /api/admin/overview` 返回 200；Playwright Chromium 检查 `/admin#training` 单模型/多模型桌面和多模型移动端无卡片、日期字段和 tooltip 溢出，并扫描后台 8 个菜单桌面/移动端无页面级横向撑宽。
- [x] 2026-06-05：排查训练页点击后“没反应”，确认原因是历史回填后立即触发多模型训练和单模型训练，旧实现把长训练请求同步占住服务进程；等待本轮训练完成并激活 `model_20260605_170301_0a2a4a` 后，新增训练接口工作线程执行和前端持续忙碌状态，重启服务后 `GET /admin`、`GET /api/admin/overview` 返回 200，静态资源版本为 `20260605-busy-1`，`node --check app/static/admin.js`、`python -m compileall app modeling tests`、`python -m pytest -q` 通过，20 个用例全部通过。
- [x] 2026-06-05：补充长任务进度条后，`node --check app/static/admin.js`、`python -m compileall app modeling tests`、`python -m pytest -q` 通过，20 个用例全部通过；`GET /admin`、`GET /api/admin/overview` 返回 200，静态资源版本为 `20260605-progress-1`；Playwright Chromium 桌面/移动端验证进度条、百分比、已等待时间和说明文案可见且不撑宽页面。
- [x] 2026-06-05：模型后台全界面样式复查后，`node --check app/static/admin.js`、`python -m compileall app modeling tests`、`python -m pytest -q` 通过，20 个用例全部通过；`GET /admin`、`GET /api/admin/overview` 返回 200，静态资源版本为 `20260605-ui-audit-4`；Playwright Chromium 点击扫描 8 个菜单在桌面、1024 宽和平板、390 宽移动端均无页面级横向溢出，`help-tip[title]` 为 0，tooltip 统一深色浮层且在屏幕内，特征权重数值列固定为 86px。
- [x] 2026-06-05：按用户要求再次清空 `data/` 运行数据，服务重启后空库自动初始化；确认 `market_rows`、`cached_payloads`、`sync_state`、`ml_models`、`ml_training_runs`、`ml_pipeline_runs`、`ml_predictions`、`ml_validation_results` 均为 0，`GET /admin`、`GET /api/admin/overview`、`GET /api/status` 返回 200；Playwright Chromium 空库状态下扫描 8 个菜单桌面、1024 宽和平板、390 宽移动端无页面级横向溢出，工作台提示“先回填历史行情”。
- [x] 2026-06-05：修正工作台“新手步骤”文本间距后，`node --check app/static/admin.js` 通过，`GET /admin` 返回 200，静态资源版本为 `20260605-text-spacing-2`；Playwright Chromium 验证 2048 宽、1440 宽、1024 宽和 390 宽工作台步骤卡片无页面级横向溢出，状态徽标与标题文字列对齐，状态到正文保持 14px 间距。
- [x] 2026-06-05：去除长任务重复提示后，`node --check app/static/admin.js` 通过，`GET /admin` 返回 200，静态资源版本为 `20260605-long-task-toast-1`；Playwright Chromium 拦截回填接口模拟长任务，确认进度卡运行中和完成时 `#toast` 均未显示，页面只保留一处任务进度反馈。

## 当前风险

- `app/api/routes/market.py` 已降到约 426 行，但仍保留少量兼容包装函数，后续可继续收敛到纯路由层。
- 个股分析已区分 `requested_end_date` 和 `actual_trade_date`，前端仍可进一步展示“已回退到实际行情日”的提示。
- migrations 目前是 MVP，只有前向 SQL 执行和版本记录，暂未提供回滚、校验和复杂数据迁移模板。
- 模型生命周期已有注册、审批、拒绝、激活、下线、归档、审计、回滚和效果监控 MVP，但还没有灰度策略、权限控制和自动告警。
- 模型训练流水线已升级为轻量逻辑回归排序器，但尚未接入 LightGBM/XGBoost/LSTM/Transformer 等更强模型和模型解释报告。
- 当前已补齐最近 40 个完整交易日的全市场日线缓存，并提供后台历史回填入口；更长周期全市场历史仍需要按 Tushare 限流分批补齐，滚动验证和跨行情阶段稳定性还需要继续观察。
- 每日推理和次日验证已有 Top N 排序指标 MVP，但尚未接入定时任务、失败重试、监控告警和前端效果曲线。
- 模型后台已改为左侧菜单、右侧内容的管理系统布局，并补充模型指标图表；后续仍可继续补权限控制、告警和更明确的风险提示。
- 主看盘台已改为独立后台模块布局，后续可继续补充表格排序、字段筛选、模型预测解释曲线和键盘快捷筛选。
- 管理接口已限制本机访问并支持可选 `ADMIN_TOKEN`，但尚未做完整用户体系；如未来部署到局域网或公网，需要补登录、权限和 CSRF 策略。
- 自动化测试已覆盖核心入口、生命周期 API、训练流水线、每日推理和次日验证 MVP。
